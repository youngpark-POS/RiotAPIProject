# version: '3.8'

services:
  web:
    build: .
    command: gunicorn --bind 0.0.0.0:8000 RiotAPIproject.wsgi:application
    volumes:
      - ./:/app
      - static_volume:/app/staticfiles
    expose:
      - "8000"

  nginx:
    image: nginx:latest
    ports:
      - "8000:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - static_volume:/app/staticfiles
    depends_on:
      - web

volumes:
  static_volume: